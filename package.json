{
  "name": "bunch-of-quant-strategies",
  "private": true,
  "scripts": {
    "dev": "node dev-server.js",
    "start": "node dev-server.js",
    "deploy": "wrangler pages deploy .",
    "preview": "wrangler pages dev ."
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
